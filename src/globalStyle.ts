import { global } from "@stitches/react";

export const globalStyle = global({
  "html, body, #root": {
    margin: 0,
    width: "100%",
    height: "100%",
    overflow: "hidden",
    fontFamily: "Arial",
    backgroundColor: "black",
  },
});
