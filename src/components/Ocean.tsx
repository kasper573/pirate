import { styled } from "@stitches/react";
import waterImageUrl from "../assets/water.gif";

export const Ocean = styled("div", {
  width: "100%",
  height: "100%",
  background: `url(${waterImageUrl})`,
});
